This directory contains legacy scripts that are kept only for historical reference.

- `create_sequences.py`: legacy sequence extraction (resizes full frames, no person-cropping).
- `extract_frames.py`: legacy frame extraction (resizes full frames, no person-cropping).

Do NOT use these scripts for building the dataset. Use `build_dataset.py` instead.
